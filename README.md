# DesarrolloSoftware
Ejercicio 1
```
* El primer caso retorna el booleano true
* El segundo caso retorna el booleano false
* El error off-by-one indica que no se está usando todo el array, solo trabaja con los primeros 31 valores
* 2 casos de prueba: 
    -primer caso de prueba sería cuando el parámetro tiene longitud 0, es decir sin contener ningun valor bit
    -primer caso de prueba sería cuando el parámetro sobrepasa su longitud de 31, saldría error por como está 
    el código. Sin embargo si se corrige para evitar el error nro     3 , este caso ya no tendría el límite
    de longitud 31 sino 32.
```

Ejercicio 2
```
¿Qué ideas demuestra esta historia?
  -Probar todas las entradas posibles es la mejor solución a este problema.
  -La verificación de tipos estáticos podría haber detectado este error.
```

Ejercicio 3
```
Un conjunto de pruebas es correcto si
  -Todos sus casos de prueba fallan cuando se ejecutan en una implementación con errores.
  -Todos sus casos de prueba pasan cuando se ejecutan en una implementación legal.
```

Ejercicio 4
```
Un conjunto de pruebas vacío no contiene casos de prueba. Suponiendo una especificación no trivial, un conjunto de pruebas vacío es:
  -Correcto.
  -Pequeña.
```

Ejercicios 5
```
¿Son los subdominios propuestos separados y completos, formando así una partición?
  La única partición acepta es:
  // particion: x < 0; x >= 0
  Ya que cumple con los requisitos disjuntos, completos y correctos. Los otros 2 casos excluyen a los negativos para  
  realizar las pruebas correspondientes.
¿Son correctos, en el sentido de que cada subdominio puede ser cubierto por un caso de prueba legal? 
  Sola la primera particón.
```

Ejercicios 6
```
// particion: x e y no son  0
  Los subdominios son disjuntos
// particion: x es divisible por y; y es divisible por x; x e y son primeros relativos
  Los subdominios son correctos
```

Ejercicios 7
```

```

Ejercicios 8
```
 -particion en a: 0
 -particion en a: 0, positivo, negativo
 -particion en a: positivo, negativo
 -particion en a: (donde "pequeño" encaja en long y "grande" no)
```

Ejercicios 9
```

```

Ejercicios 10
```

```














